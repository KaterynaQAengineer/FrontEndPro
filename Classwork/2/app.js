
 //2 Вивести на сторінку квадрати чисел від 10 до 20 через кому

    
for (let i = 10; i<=20; i++ ) {
    let b = i*i;
    if (i<20) {
        document.write(`${b}, `);
    }
   else 
   document.write(`${b}`);
    }